# Files and directories excluded from the release zip.
# vendor/ is intentionally absent — it is built during the release workflow and included in the zip.

.DS_Store
.distignore
.git
.github
.gitignore
.phpunit.cache
blocks/markdown/src
composer.json
composer.lock
node_modules
package.json
package-lock.json
phpunit.xml
tests
