{
    "name": "wp-agent-memory",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "wp-scripts build src/block-bindings.js --output-path=build",
        "start": "wp-scripts start src/block-bindings.js --output-path=build"
    },
    "devDependencies": {
        "@wordpress/scripts": "^30.0.0"
    }
}
